package ${packageName}.service.${moduleName};

import com.crcm.cloud.start.data.mybatis.bean.PageT;
import com.baomidou.mybatisplus.extension.service.IService;
import java.util.List;
import ${packageName}.model.entity.${moduleName}.${ClassName};

/**
 * ${functionName}Service接口
 * 
 * @author ${author}
 * @date ${datetime}
 */
public interface ${ClassName}Service extends IService<${ClassName}>{

    /**
     * 新增${functionName}
     * 
     * @param ${className} ${functionName}
     * @return 结果
     */
    boolean save${ClassName}(${ClassName} ${className});

    /**
     * 修改${functionName}
     * 
     * @param ${className} ${functionName}
     * @return 结果
     */
    boolean update${ClassName}(${ClassName} ${className});

    /**
     * 删除${functionName}信息
     * 
     * @param ${pkColumn.javaField} ${functionName}ID
     * @return 结果
     */
    boolean delete${ClassName}(${pkColumn.javaType} ${pkColumn.javaField});

    /**
     * 查询${functionName}
     *
     * @param ${pkColumn.javaField} ${functionName}ID
     * @return ${functionName}
     */
    ${ClassName} find${ClassName}ById(${pkColumn.javaType} ${pkColumn.javaField});

    /**
     * 查询${functionName}列表
     *
     * @param ${className} ${functionName}
     * @return ${functionName}集合
     */
    List<${ClassName}> find${ClassName}List(${ClassName} ${className});

    /**
     * 分页查询${functionName}列表
     * @param page 分页参数
     * @param ${className} ${functionName}
     * @return ${functionName}集合
     */
    PageT<${ClassName}> find${ClassName}Page(PageT<${ClassName}> page, ${ClassName} ${className});
}
